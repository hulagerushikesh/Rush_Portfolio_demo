# Supabase project settings (Settings -> API in the Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: SMTP creds for a best-effort email notification when the contact
# form is submitted. If unset, messages are still saved to the database.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
CONTACT_NOTIFY_EMAIL=hulagerushikesh@gmail.com
